VITE_BACKEND_URL=http://localhost:3003

# SPOC backend — used for final-team SSE events on the participant dashboard
# Set to the deployed SPOC backend URL in production
VITE_SPOC_BACKEND_URL=http://localhost:3004
