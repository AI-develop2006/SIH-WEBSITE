{
  "name": "sih-portal-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && node build-sync.js",
    "start": "npm run start --prefix backend"
  }
}
